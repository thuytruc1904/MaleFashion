server.port=8080
spring.datasource.url=jdbc:mysql://localhost:3306/fashionstore?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD}  # Thay đổi mật khẩu này

#Hibernate
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

#Hibernate auto dd
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
#
logging.level.org.hibernate.SQL=DEBUG

spring.servlet.multipart.max-file-size = 5MB
spring.servlet.multipart.max-request-size = 5MB

#Cloudinary configuration
cloudinary.cloud_name=djyw3ytjd
cloudinary.api_key=${CLOUDINARY_API_KEY}  # Thay đổi API key này
cloudinary.api_secret=${CLOUDINARY_API_SECRET}  # Thay đổi bí mật này

#Send email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=hoang1521065@huce.edu.vn
spring.mail.password=${MAIL_PASSWORD}  # Thay đổi mật khẩu email này
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
mail.smtp.debug=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

#SignIn-Google
#google.app.id=${GOOGLE_APP_ID}  # Thay đổi ID này
#google.app.secret=${GOOGLE_APP_SECRET}  # Thay đổi bí mật này
#google.redirect.uri=http://localhost:8080/login-google
#google.link.get.token=https://accounts.google.com/o/oauth2/token
#google.link.get.user_info=https://www.googleapis.com/oauth2/v1/userinfo?access_token=